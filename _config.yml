# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you: 
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: ehime IT
email: your-email@example.com
# jekyll-seo-tag はトップページの <title> を「site.title | tagline(未設定ならdescription)」で
# 組み立てる。tagline が無いと description 全文がtitleに流用され約70文字になるため短い語を置く。
# architectテーマのヘッダに表示されるのは description の方で、tagline は画面に出ない
tagline: 愛媛のITコミュニティ
description: >- # this means to ignore newlines until "baseurl:"
  愛媛県のITエンジニア・デザイナー・学生・経営者が集まるコミュニティ。Slackグループと地域の勉強会の情報を紹介しています。
baseurl: "" # the subpath of your site, e.g. /blog
# サイトの絶対URL。ここが空だと absolute_url が機能せず、og:url / canonical / feed.xml の
# URLがすべて相対値になる（GitHub Pages のユーザーページなので baseurl は空のままでよい）
url: "https://ehime-it.github.io"
# テーマのlayoutが <html lang="{{ site.lang | default: "en-US" }}"> を出力するため、
# 指定しないと日本語サイトが en-US を宣言してしまう
lang: ja
twitter_username: ramusara
github_username:  ehime-it

# Build settings
theme: jekyll-theme-architect
plugins:
  - jekyll-feed
  - jekyll-sitemap

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
# exclude:
#   - .sass-cache/
#   - .jekyll-cache/
#   - gemfiles/
#   - Gemfile
#   - Gemfile.lock
#   - node_modules/
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/
